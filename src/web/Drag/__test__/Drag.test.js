import * as React from 'react';
import { shallow } from 'enzyme';
import Drag from '../index';

describe('<Drag />', () => {
	it('render Drag with Drag', () => {
		// const wrapper = shallow(<Drag text={"Drag"} />).children();
		// expect(wrapper.text()).toBe('Drag');
	});
});